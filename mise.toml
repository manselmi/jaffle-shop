# vim: set ft=toml :


# https://github.com/jdx/mise/pull/13002
min_version = '2026.9.4'


[settings]
locked = true
lockfile = true
lockfile_platforms = [
  'linux-arm64',
  'linux-x64',
  'macos-arm64',
]

[settings.python]
compile = false


[task_config]
shell = 'sh -euo pipefail -c'


[tasks.all]
alias = 'default'
run = [
  { task = 'lint' },
  { task = 'dbt-build' },
  { task = 'mf' },
]

[tasks.dbt-build]
depends = [
  'jafgen',
  'dbt-deps',
]
run = 'pixi run -- dbt build'

[tasks.dbt-compile]
depends = [
  'dbt-deps',
]
run = 'pixi run -- dbt compile'

[tasks.dbt-deps]
outputs = [
  'dbt_packages',
]
run = 'pixi run -- dbt deps'
sources = [
  'dependencies.yml',
  'package-lock.yml',
]

[tasks.jafgen]
outputs = [
  'jaffle-data/*.csv',
]
run = 'pixi run -- jafgen'
sources = [
  'jaffle-data/.gitignore',
]

[tasks.lint]
depends = [
  'dbt-compile',
]
run = """\
if test -n "${CI:-}"; then \
  CMD='sqlfluff lint'; \
else \
  CMD='sqlfluff fix --show-lint-violations'; \
fi; \
pixi run -e lint -- ${CMD}"""

[tasks.lock]
# required to correctly initialize lockfile when locked = true
env.MISE_LOCKED = '0'
run = [
  'mise install',
  'mise lock',
  'pixi lock',
  { task = 'dbt-deps' },
]

[tasks.mf]
run = [
  { task = 'mf-health-checks' },
  { task = 'mf-validate-configs' },
]

[tasks.mf-health-checks]
run = 'pixi run -- mf health-checks'

[tasks.mf-validate-configs]
run = 'pixi run -- mf validate-configs --show-all --verbose-issues'

[tasks.upgrade]
# required to correctly initialize lockfile when locked = true
env.MISE_LOCKED = '0'
run = [
  'mise upgrade --local',
  'mise lock',
  'pixi update',
  'pixi run -- dbt deps --upgrade',
]


[tools]
pixi = 'latest'
