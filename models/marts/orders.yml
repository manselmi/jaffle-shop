models:
  - name: orders
    description: Order overview data mart, offering key details for each order inlcluding if it's a customer's first order and a food vs. drink item breakdown. One row per order.
    semantic_model:
      enabled: true
    agg_time_dimension: ordered_at
    data_tests:
      - dbt_utils.expression_is_true:
          arguments:
            expression: "order_items_subtotal = subtotal"
      - dbt_utils.expression_is_true:
          arguments:
            expression: "order_total = subtotal + tax_paid"
    columns:
      - name: order_id
        description: The unique key of the orders mart.
        data_tests:
          - not_null
          - unique
        entity:
          type: primary
          name: order_id
      - name: customer_id
        description: The foreign key relating to the customer who placed the order.
        data_tests:
          - relationships:
              arguments:
                to: ref('stg_customers')
                field: customer_id
        entity:
          type: foreign
          name: customer
      - name: location_id
        entity:
          type: foreign
          name: location
      - name: order_total
        description: The total amount of the order in USD including tax.
        dimension:
          type: categorical
          name: order_total_dim
      - name: ordered_at
        description: The timestamp the order was placed at.
        granularity: day
        dimension:
          type: time
      - name: order_cost
        description: The sum of supply expenses to fulfill the order.
      - name: is_food_order
        description: A boolean indicating if this order included any food items.
        dimension:
          type: categorical
      - name: is_drink_order
        description: A boolean indicating if this order included any drink items.
        dimension:
          type: categorical
      - name: customer_order_number
        dimension:
          type: categorical
    metrics:
      - name: order_total
        description: Sum of total order amount. Includes tax + revenue.
        label: Order Total
        type: simple
        agg: sum
      - name: orders
        description: Count of orders.
        label: Orders
        type: simple
        agg: sum
        expr: 1
      - name: new_customer_orders
        description: New customer's first order count
        label: New Customers
        type: simple
        agg: sum
        expr: 1
        filter: |
          {{ Dimension('order_id__customer_order_number') }} = 1
      - name: large_orders
        description: "Count of orders with order total over 20."
        label: Large Orders
        type: simple
        agg: sum
        expr: 1
        filter: |
          {{ Dimension('order_id__order_total_dim') }} >= 20
      - name: food_orders
        description: Count of orders that contain food order items
        label: Food Orders
        type: simple
        agg: sum
        expr: 1
        filter: |
          {{ Dimension('order_id__is_food_order') }} = true
      - name: drink_orders
        description: Count of orders that contain drink order items
        label: Drink Orders
        type: simple
        agg: sum
        expr: 1
        filter: |
          {{ Dimension('order_id__is_drink_order') }} = true
      - name: tax_paid
        description: The total tax paid on each order.
        label: Tax Paid
        type: simple
        agg: sum
      - name: order_cost
        description: The cost for each order item. Cost is calculated as a sum of the supply cost for each order item.
        label: Order Cost
        type: simple
        agg: sum

saved_queries:
  - name: order_metrics
    description: Key order metrics grouped by day.
    label: Order Metrics
    query_params:
      metrics:
        - orders
        - new_customer_orders
        - order_total
        - food_orders
        - drink_orders
      group_by:
        - TimeDimension('metric_time', 'day')
    exports:
      - name: order_metrics
        config:
          export_as: table

unit_tests:
  - name: test_order_items_compute_to_bools_correctly
    description: "Test that the counts of drinks and food orders convert to booleans properly."
    model: orders
    given:
      - input: ref('order_items')
        rows:
          - {
              order_id: "69db5001-b2b5-4bb5-af3d-f1fc9834d687",
              order_item_id: "7133947b-6d9d-4ed7-b070-b4ec3f83f2ac",
              is_drink_item: false,
              is_food_item: true,
            }
          - {
              order_id: "69db5001-b2b5-4bb5-af3d-f1fc9834d687",
              order_item_id: "f4e5f789-d713-4417-9f47-539442dd1df3",
              is_drink_item: true,
              is_food_item: false,
            }
          - {
              order_id: "dda2e9e0-b07d-4f98-b991-6618e3a2a7c7",
              order_item_id: "fdf6bb2c-1f2a-4588-80d4-44de178bff9f",
              is_drink_item: false,
              is_food_item: true,
            }
      - input: ref('stg_orders')
        rows:
          - { order_id: "69db5001-b2b5-4bb5-af3d-f1fc9834d687" }
          - { order_id: "dda2e9e0-b07d-4f98-b991-6618e3a2a7c7" }
    expect:
      rows:
        - {
            order_id: "69db5001-b2b5-4bb5-af3d-f1fc9834d687",
            count_food_items: 1,
            count_drink_items: 1,
            is_drink_order: true,
            is_food_order: true,
          }
        - {
            order_id: "dda2e9e0-b07d-4f98-b991-6618e3a2a7c7",
            count_food_items: 1,
            count_drink_items: 0,
            is_drink_order: false,
            is_food_order: true,
          }
